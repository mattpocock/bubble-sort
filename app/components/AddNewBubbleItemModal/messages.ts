/*
 * AddNewBubbleItemModal Messages
 *
 * This contains all the text for the AddNewBubbleItemModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddNewBubbleItemModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddNewBubbleItemModal component!',
  },
});
