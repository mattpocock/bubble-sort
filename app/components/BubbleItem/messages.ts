/*
 * BubbleItem Messages
 *
 * This contains all the text for the BubbleItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BubbleItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BubbleItem component!',
  },
});
